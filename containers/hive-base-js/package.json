{
  "name": "hive-rest-js",
  "version": "2.0.0-beta.2",
  "description": "Starter kit for Hive Framework REST services in Node.js w/ Micro implementations in Docker",
  "main": "./bin/start",
  "scripts": {
    "prebuild": "del dist",
    "build": "babel src --out-dir dist",
    "coverage": "NODE_ENV=test nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "dev": "nodemon -e js,json -w src -w conf -x \"npm run start:dev\" & nodemon -e js -w tst -x \"npm test\"",
    "fix": "standard --fix",
    "prerelease": "del app.tar.gz",
    "release": "NODE_ENV=production npm test && npm run build && node ./bin/compress",
    "start": "NODE_ENV=production node ./bin/start",
    "start:dev": "NODE_ENV=development npm run build && node ./bin/start",
    "pretest": "del coverage coverage.lcov .nyc_output",
    "test": "standard",
    "posttest": "NODE_ENV=test nyc --reporter=html --reporter=text mocha --recursive --exit 'tst/**/*.spec.js'",
    "version": "npm run release && git add --all",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fnalabs/hive-rest-js.git"
  },
  "bugs": {
    "url": "https://github.com/fnalabs/hive-rest-js/issues"
  },
  "homepage": "https://github.com/fnalabs/hive-rest-js#readme",
  "author": "Adam Eilers",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=8",
    "npm": ">=5"
  },
  "dependencies": {
    "micro": "~9.3.1"
  },
  "devDependencies": {
    "archiver": "~2.1.0",
    "babel-cli": "~6.26.0",
    "babel-core": "~6.26.3",
    "babel-eslint": "~8.2.3",
    "babel-plugin-add-module-exports": "~0.2.1",
    "babel-plugin-istanbul": "~4.1.6",
    "babel-plugin-syntax-object-rest-spread": "~6.13.0",
    "babel-plugin-transform-es2015-modules-commonjs": "~6.26.2",
    "babel-register": "~6.26.0",
    "chai": "~4.1.2",
    "chai-as-promised": "~7.1.1",
    "chai-http": "~4.0.0",
    "codecov": "~3.0.2",
    "del-cli": "~1.1.0",
    "dirty-chai": "~2.0.1",
    "mocha": "~5.2.0",
    "nodemon": "~1.17.5",
    "nyc": "~11.8.0",
    "proxyquire": "~2.0.1",
    "sinon": "~5.0.10",
    "standard": "~11.0.1"
  },
  "nyc": {
    "check-coverage": true,
    "per-file": true,
    "lines": 80,
    "statements": 80,
    "functions": 80,
    "branches": 80,
    "watermarks": {
      "lines": [
        80,
        90
      ],
      "functions": [
        80,
        90
      ],
      "branches": [
        80,
        90
      ],
      "statements": [
        80,
        90
      ]
    },
    "require": [
      "babel-register"
    ],
    "sourceMap": false,
    "instrument": false
  },
  "standard": {
    "parser": "babel-eslint"
  }
}
